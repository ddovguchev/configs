{
  aliens = {
    path = ./default/aliens.omp.json;
    note = "Минимализм";
  };

  atomic = {
    path = ./default/atomic.omp.json;
    note = "Показывает AWS и Kubernetes контексты";
  };

  gmay = {
    path = ./default/gmay.omp.json;
    note = "Можно перенять стиль и иконки";
  };

  jandedobbeleer = {
    path = ./default/jandedobbeleer.omp.json;
    note = "Визуально топ, но надо убрать zsh и добавить AWS/K8s";
  };

  json = {
    path = ./default/json.omp.json;
    note = "Просто норм, usable";
  };

  remk = {
    path = ./default/remk.omp.json;
    note = "Визуально приятная";
  };

  dd = {
    path = ./custom/dd.omp.json;
    note = "";
  };
}
